"use client";

export default function AdSlot({
  slot: _slot,
  className: _className,
}: {
  slot: string;
  className?: string;
}) {
  // Reserved for future Google AdSense integration
  return null;
}
